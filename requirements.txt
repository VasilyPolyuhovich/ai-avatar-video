# Local tooling dependencies only -- NOT used inside the Docker images
# (docker/*/Dockerfile each pin their own requirements independently).
#
# scripts/pod_up.py and scripts/generate_avatar_video.py are deliberately
# stdlib-only (raw ssh/scp via subprocess, matching this project's proven
# pattern -- no paramiko). gradio is the only dependency here, scoped to
# scripts/app_gradio.py alone.
#
# >=4.38 specifically: scripts/app_gradio.py uses gr.Timer (added in 4.38)
# to poll session-pod status for the browser without requiring a click.
gradio>=4.38,<6.0
